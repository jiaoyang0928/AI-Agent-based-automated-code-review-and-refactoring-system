# AI-Agent-based-automated-code-review-and-refactoring-system
A multi-agent system that automates code review and refactoring. It scans code diffs for technical debt (e.g., hardcoded secrets, leftover TODOs, debug statements), matches violations against architectural rules, generates structured refactor plans and PR descriptions, then runs unit tests in a sandbox for closed-loop verification.
